apiVersion: v1
kind: Service
metadata:
  name: legacy
  namespace: default
  labels:
    app: legacy
spec:
  selector:
    app: legacy
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
